import Nav from "@components/core/Nav";

const Data = () => {
  return (
    <div className="flex flex-col items-center h-screen bg-black">
      <Nav />
    </div>
  );
};

export default Data;
